function UserInfo() {
    return (
        <div>
            <h1>User Info Page</h1>
            <p>a tab in NavBar?</p>
            <p>lists user info (name etc.) and history of emissions requests and summary of emissions use</p>
        </div>
    )
}

export default UserInfo;