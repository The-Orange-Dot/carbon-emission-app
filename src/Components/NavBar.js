function NavBar() {
    return (
        <div>
            <h1>NavBar</h1>
            <p>user icon at the top with username and logout button</p>
            <p>tabs to bring up forms etc to GET data from API and POST response to user info in db.json</p>
        </div>
    )
}

export default NavBar;